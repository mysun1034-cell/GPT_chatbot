"""수업 슬라이드를 트리로 펼친 자료. 앱이 설명·퀴즈 주제로 쓴다."""

LECTURES = [
    {
        "id": "core01",
        "track": "LLM 핵심기술",
        "title": "Day 1 · 어텐션이 등장한다",
        "date": "7/28",
        "one_liner": "문장 전체를 벡터 하나에 눌러 담으면 앞말이 뭉개진다. 그걸 푸는 장치가 어텐션이다.",
        "explain": """식당에서 주문 10개를 종업원 머릿속 **메모 한 장**에 우겨 넣는다고 생각해 봐.
앞 테이블 주문은 흐려지고, 마지막 테이블만 선명해진다. 그게 RNN의 **컨텍스트 벡터 병목**이야.

어텐션은 메모 한 장 대신, 손님이 말할 때마다 **원장 전체를 다시 훑고**
‘지금 이 말에 중요한 칸’에만 가중치를 준다.

- 점수를 매기고 (어디에 눈을 줄까)
- 비율로 바꾸고 (softmax, 합이 1)
- 그 비율로 내용을 섞는다 (가중합)

번역처럼 **두 문장 사이**를 볼 때가 어제 그림의 cross-attention이다.
핵심 한 줄: **멀리 있는 단어도 한 번에 만날 수 있다.**""",
        "quiz_topic": "RNN 컨텍스트 벡터 병목과 어텐션이 등장한 이유. 점수-softmax-가중합.",
        "items": [
            {"id": "bottleneck", "title": "컨텍스트 벡터 병목", "body": "RNN은 문장 전체를 벡터 하나에 눌러 담는다. 앞 주문은 흐려지고 마지막만 선명하다."},
            {"id": "score", "title": "점수 매기기", "body": "지금 이 말에 어떤 칸이 중요한지 짝마다 점수를 낸다."},
            {"id": "softmax", "title": "softmax 비율", "body": "점수를 비율로 바꾼다. 한 자리가 나눠 보는 합이 1이 되어야 한다."},
            {"id": "weighted", "title": "가중합", "body": "그 비율로 내용을 섞는다. 멀리 있는 단어도 한 번에 만난다."},
            {"id": "cross", "title": "cross-attention", "body": "번역처럼 두 문장 사이를 볼 때. Q는 한쪽, K·V는 다른 쪽."},
        ],
    },
    {
        "id": "core02",
        "track": "LLM 핵심기술",
        "title": "Day 2 · 셀프 어텐션과 멀티헤드",
        "date": "7/29",
        "one_liner": "한 문장 안에서 단어끼리 서로를 본다. 공식은 softmax(QKᵀ/√d_k)V 한 줄이다.",
        "explain": """같은 한글 ‘배’가 **아프다 / 타다 / 먹다** 로 갈린다.
혼자서는 뜻이 안 정해지고, **같은 문장의 다른 단어**를 봐야 정해진다. 그게 셀프 어텐션이다.

역할을 셋으로 나눈다.

- **Q (질의)**: 나는 지금 무엇을 찾고 있나
- **K (이름표)**: 나는 무엇인가
- **V (내용)**: 내가 들고 있는 정보

공식: `softmax(QKᵀ / √d_k) V`

1. 짝마다 점수 (행렬곱 한 번이 L²번을 대신한다)
2. √d_k 로 나눠 분산을 되돌린다. 안 나누면 softmax가 한곳에 포화된다
3. 행 방향으로 softmax — 한 자리가 나눠 보는 비율의 합이 1
4. 그 비율로 V를 섞는다

**멀티헤드**: 가중합을 여러 조각(헤드)으로 나눠 병렬로 보고 이어 붙인다.
헤드를 늘려도 보통 총 차원 d를 쪼개므로 파라미터는 거의 안 늘어난다.

잃은 것: 이 계산 어디에도 **몇 번째 단어인지**가 없다. 다음이 위치 인코딩이다.""",
        "quiz_topic": "셀프 어텐션. Q K V 역할, softmax(QK^T/√d_k)V, √d_k를 나누는 이유, 멀티헤드.",
        "items": [
            {"id": "bae", "title": "배의 세 뜻", "body": "아프다/타다/먹다. 혼자서는 안 정해지고 같은 문장의 다른 단어를 봐야 한다."},
            {"id": "qkv", "title": "Q · K · V", "body": "Q는 무엇을 찾나, K는 나는 무엇인가, V는 들고 있는 내용."},
            {"id": "formula", "title": "softmax(QKᵀ/√d_k)V", "body": "점수 → 분산 되돌리기 → 비율 → 내용 섞기. 행렬곱 한 번이 L²번을 대신한다."},
            {"id": "sqrt", "title": "왜 √d_k 로 나누나", "body": "안 나누면 내적 분산이 커져 softmax가 한곳에 포화된다. √d_k는 분산을 되돌리는 값이다."},
            {"id": "mha", "title": "멀티헤드", "body": "한 문장에서 볼 것이 여러 종류면 쪼개서 병렬로 보고 이어 붙인다. 총 차원 d를 나누므로 파라미터는 거의 안 는다."},
            {"id": "pos", "title": "잃은 것 — 위치", "body": "이 계산 어디에도 몇 번째 단어인지가 없다. 다음이 위치 인코딩."},
        ],
    },
    {
        "id": "core04",
        "track": "LLM 핵심기술",
        "title": "Day 4 · BERT · GPT · Hugging Face",
        "date": "7/31",
        "one_liner": "같은 트랜스포머 블록인데, 쌓는 방식과 학습 질문이 달라 하는 일이 갈린다.",
        "explain": """어제는 구조를 짰을 뿐 **학습은 안 했다**. 오늘은 남이 학습해 둔 모델을 **가져다 쓰는 날**이다.

문장을 넣으면 처음엔 못 읽는 숫자가 나온다. 몸통은 **표현(토큰당 768)** 까지만 계산하고,
단어 점수로 바꾸는 마지막 층이 **머리(head)** 다.

특수 토큰:

- `[CLS]` 문장 맨 앞, 문장 전체를 대표
- `[SEP]` 문장 끝 또는 두 문장 경계

가계도:

| | BERT | GPT | BART |
|---|---|---|---|
| 쌓기 | 인코더만 | 디코더만 | 양쪽 |
| 학습 | 가린 단어 맞히기 | 다음 단어 맞히기 | 망가뜨린 문장 복원 |

디코더가 인코더와 다른 점 둘: **뒤를 못 본다**(causal mask), **인코더를 본다**(cross-attention).

한국어 예: `klue/bert-base`, `skt/kogpt2-base-v2`, KoBART.
주소는 `만든사람/모델이름`. 라이선스는 모델 카드부터.

단어 수십만을 32,000칸에 넣는 법 = **서브워드**. 자주 나오는 덩어리는 통째, 드문 말은 조각.
`##` 는 앞조각에 이어 붙는다는 표기다. 목록에 없는 글자는 `[UNK]`.

bertviz로 ‘배’가 아픈지 타는지를 **어느 헤드가 어디를 보는지** 눈으로 확인할 수 있다.
요약(KoBART)은 인코더가 읽고 디코더가 짧게 쓴다.""",
        "quiz_topic": "BERT GPT BART 차이. 인코더/디코더, [CLS][SEP], 서브워드, 머리(head)와 표현(768).",
        "items": [
            {"id": "repr", "title": "표현과 머리", "body": "몸통은 토큰당 768 표현까지. 단어 점수로 바꾸는 마지막 층이 머리(head)다."},
            {"id": "special", "title": "[CLS] · [SEP]", "body": "[CLS]는 문장 맨 앞·전체 대표. [SEP]는 끝 또는 두 문장 경계."},
            {"id": "family", "title": "가계도 BERT/GPT/BART", "body": "BERT 인코더만·가린 단어. GPT 디코더만·다음 단어. BART 양쪽·문장 복원."},
            {"id": "decoder2", "title": "디코더가 다른 점", "body": "뒤를 못 본다(causal mask). 인코더를 본다(cross-attention)."},
            {"id": "subword", "title": "서브워드", "body": "자주 나오는 덩어리는 통째, 드문 말은 조각. ##는 이어 붙임. 없으면 [UNK]."},
            {"id": "hub", "title": "허브와 라이선스", "body": "주소는 만든사람/모델이름. 쓰기 전에 모델 카드의 라이선스부터."},
        ],
    },
    {
        "id": "core05",
        "track": "LLM 핵심기술",
        "title": "Day 5 · 생성 = 다음 한 토큰의 반복",
        "date": "8/13",
        "one_liner": "모델은 확률만 매긴다. 고르는 방법은 우리가 정한다.",
        "explain": """BERT는 빈칸을 채웠다. GPT는 **다음 한 토큰**을 고른다.
폰 키보드 추천과 같다. 지금까지 친 글자 → 다음 한 단어 후보.

문장을 넣으면 마지막 자리의 점수가 어휘 개수만큼 나온다(KoGPT2는 약 5만).
softmax로 확률. 상위 5개를 다 더해도 37.9%일 수 있다. 확률이 얇게 흩어져 있다.

손으로 세 번: 1등 고르기 → 이어 붙이기 → 다시 모델에 넣기.
`generate()` 는 이 반복문이다. 새로운 일이 아니다.

**greedy**(항상 1등): 결정적이고, “바로 이 집은 바로 이 산속…”처럼 **맴돈다**.
탈출: 반복 벌칙(`repetition_penalty`), 또는 1등만 고르지 않기.

- `temperature` 낮으면 분포가 뾰족(안전하지만 뻔함). 높으면 평평(다양, 이상해질 수 있음)
- `top-k`: 상위 k개만
- `top-p`: 확률을 더해 p에 닿을 때까지

정답이 하나인 일(번역·요약)은 greedy가 맞는 자리가 있다.
그런데 “오늘 아침 날씨”를 greedy로 뽑아도 **모델은 오늘 날씨를 모른다**. 그럴듯한 예보문 = 환각.

오후에는 남의 컴퓨터 모델(OpenAI API). 키는 코드에 적지 말고 `.env`.
남은 한계 둘: 환각, 컨텍스트 윈도우. 관련 부분만 찾아 넣으면 RAG.""",
        "quiz_topic": "생성 디코딩. greedy 붕괴, temperature, top-k top-p, 모델은 확률만 매긴다, 환각.",
        "items": [
            {"id": "next", "title": "다음 한 토큰", "body": "폰 키보드처럼 지금까지 친 글자 → 다음 후보. generate()는 고르기·붙이기·다시 넣기의 반복이다."},
            {"id": "greedy", "title": "greedy 붕괴", "body": "항상 1등이면 결정적이고 맴돈다. ‘바로 이 집은 바로 이 산속…’"},
            {"id": "temp", "title": "temperature", "body": "낮으면 뾰족(안전하지만 뻔함). 높으면 평평(다양, 이상해질 수 있음). 확률을 T로 나눈다."},
            {"id": "topk", "title": "top-k / top-p", "body": "꼬리의 이상한 토큰을 자른다. k는 개수 고정, p는 확률 합 고정."},
            {"id": "hallu", "title": "환각", "body": "날씨 문장을 그럴듯하게 써도 모델은 오늘 날씨를 모른다."},
            {"id": "limit", "title": "남은 한계", "body": "환각과 컨텍스트 윈도우. 관련 부분만 찾아 넣으면 RAG."},
        ],
    },
    {
        "id": "api01",
        "track": "LLM API",
        "title": "Day 1 · 대화는 배열이다",
        "date": "8/14",
        "one_liner": "서버는 대화를 저장하지 않는다. 앞 대화를 내가 쌓아 매번 통째로 다시 보낸다.",
        "explain": """종업원은 테이블을 비우면 주문을 잊는다. “다음에 기억할게요”는 인사치레다.
주문서를 **손님이 들고 있어야** 다음에도 이어진다. 그게 **무상태(stateless)** 다.

`messages` 조각의 신분은 셋이다.

- `system` 지시. 배열 맨 앞. 무대 뒤
- `user` 내가 한 말
- `assistant` 모델의 답

같은 질문(“감기는 왜 걸려?”)에 system만 바꾸면 초등 선생님 / 논문 저자 / 어의가 된다.

호출을 두 번 따로 내면 “홍길동 기억해” 다음 “내 이름이 뭐야”에 모른다.
해결: 답도 append하고 배열 통째로 다시 보낸다. 모델의 답을 안 넣으면 모델은 자기가 한 말도 모른다.

매 턴 서버에 이어 붙이는 게 아니다. **두꺼워지는 편지를 처음부터 다시 부친다.**
받은 토큰은 비슷하고 보낸 토큰만 37→432처럼 자란다. 요금은 토큰 수. 폭주하는 쪽은 입력이다.

응답은 문자열이 아니라 객체다. 볼 자리 셋: `role`, `finish_reason`(`stop`/`length`), `usage`.
`gpt-5-nano`는 생각 토큰에 예산을 다 써서 답이 비어 `length`로 끝나기도 한다.

세 그릇은 같다. 셀 / `chat.py`의 while / Streamlit의 `session_state`.
Streamlit은 화면마다 파일을 다시 실행하므로 일반 변수는 날아간다.""",
        "quiz_topic": "OpenAI 멀티턴. messages 역할, 무상태, 보낸 토큰이 커지는 이유, finish_reason, session_state.",
        "items": [
            {"id": "stateless", "title": "무상태", "body": "서버는 대화를 저장하지 않는다. ‘기억할게요’는 약속이 아니라 문장 생성이다."},
            {"id": "roles", "title": "role 셋", "body": "system 지시(맨 앞), user 나, assistant 모델의 답."},
            {"id": "persona", "title": "페르소나", "body": "같은 질문에 system만 바꾸면 초등 선생님/논문 저자/어의가 된다."},
            {"id": "resend", "title": "통째로 다시 보낸다", "body": "두꺼워지는 편지를 매번 처음부터 부친다. 모델 답도 append해야 자기가 한 말을 안다."},
            {"id": "tokens", "title": "보낸 토큰이 자란다", "body": "받은 토큰은 비슷, 보낸 토큰만 37→432. 요금은 토큰 수. 폭주하는 쪽은 입력."},
            {"id": "finish", "title": "finish_reason", "body": "stop은 할 말 다 함. length는 예산 소진. nano는 생각에 예산을 다 써 답이 비기도 한다."},
            {"id": "bowls", "title": "세 그릇", "body": "셀 / chat.py while / Streamlit session_state. 배열은 같고 그릇만 다르다."},
        ],
    },
    {
        "id": "api02",
        "track": "LLM API",
        "title": "Day 2 · 구조화 출력과 도구 호출",
        "date": "8/18",
        "one_liner": "JSON 스키마로 약속을 적는 법 하나. 오전은 답의 모양, 오후는 함수 설명서.",
        "explain": """리뷰 1만 개를 사람이 읽은 분석문은 좋다. 코드는 ‘별 4~5 느낌’에서 숫자를 못 뺀다.
형식이 먼저 약속돼야 한다. 그 설명서가 **JSON 스키마**다.

세 단:

1. `json_object` — 유효한 JSON만. 키는 매번 흔들린다. 프롬프트에 JSON이라는 단어 필요(없으면 400)
2. `json_schema` + `strict` — required + additionalProperties false. 모양이 계약
3. `parse` + Pydantic — 클래스가 곧 스키마. `.parsed`가 객체

400은 양식 문제라 서버 말에 고칠 곳이 있다. 최상위 클래스 이름은 영문.

퀴즈의 `answer`가 int 데이터라서 채점 프로그램이 성립한다.

도구: 모델은 **지금 날씨**를 모른다. 좌표는 학습으로 채우고, 기온은 내 함수(open-meteo)가 잰다.

네 걸음: 설명서(`tools`) → 요청서(`tool_calls`, content=None) → 내가 실행 → `role:tool`로 재투입.
모델은 실행하지 않는다. 고르는 것도 모델이다. 도구를 실었다고 항상 부르진 않는다.

계산도 모델이 틀릴 수 있다. `eval` 전에 숫자·사칙만 통과시키는 문이 필요하다.
요청서가 여러 장이면 `for`. 상한 5바퀴. 빈 답은 대체 문구.

`tool_app.py`는 금요일 app.py와 그릇만 같고 왕복은 같다.""",
        "quiz_topic": "구조화 출력 json_object vs json_schema vs parse. 도구 호출 네 걸음, role tool, 모델은 실행하지 않는다.",
        "items": [
            {"id": "need", "title": "코드가 못 읽는 답", "body": "‘별 4~5 느낌’은 좋은 분석이지만 평균을 못 낸다. 형식이 먼저 약속돼야 한다."},
            {"id": "jobj", "title": "1단 json_object", "body": "유효한 JSON만 보장. 키는 매번 흔들린다. 프롬프트에 JSON 없으면 400."},
            {"id": "strict", "title": "2단 json_schema+strict", "body": "required + additionalProperties false + strict. 모양이 계약. description은 유일한 설명."},
            {"id": "pyd", "title": "3단 Pydantic parse", "body": "클래스가 곧 스키마. .parsed가 객체. 최상위 클래스 이름은 영문."},
            {"id": "four", "title": "도구 네 걸음", "body": "설명서 → 요청서(content None) → 내가 실행 → role tool로 재투입. 모델은 실행하지 않는다."},
            {"id": "calc", "title": "calc의 문", "body": "expression은 모델이 만든 문자열. eval 전에 숫자·사칙만 통과."},
            {"id": "loop", "title": "여러 장 · 루프", "body": "tool_calls는 리스트라 for가 필수. 상한 5바퀴. 빈 답은 대체 문구."},
        ],
    },
]

from curriculum_more import MORE_LECTURES

LECTURES = LECTURES + MORE_LECTURES
